import { Routes } from '@angular/router';
import { WorklistComponent } from './lib/worklist/worklist.component';


export const Worklist_ROUTES: Routes = [
    { path: 'worklist', component: WorklistComponent },
  ];
